/**
 * 1. create a firebase project
 * 2. create a web app
 * 3. npm i firebase
 * 4. save firebase config
 * 5. export default app
 * 6. Build> Authentication > Get Started > Enable SIgn In methods
 * 7. create signup & login route
 * */

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
 * 
 * **/

/**
 * HOSTING
 * ------------
 * One time per PC
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * For each project one time:
 * 1. firebase init
 * 2. proceed > 
 * 3. hosting: firebase (up down arrow and use space bar to select)
 * 4. existing project > select the project carefully
 * 5. which project as public directory: dist
 * 6. single page app: yes
 * 7. continuous deployment: no
 * */

/**
 * For every time deploy
 * 1. npm run build
 * 2. firebase deploy
 * */ 